import React from 'react';
import './Spinner.css';

const spinner = (props) => (
    <div className="Spinner">
        <div className="Loader">Loading...</div>
    </div>
   
);

export default spinner